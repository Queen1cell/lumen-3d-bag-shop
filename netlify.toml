# Netlify 배포 설정 — 빌드 과정이 없는 순수 정적 사이트
[build]
  publish = "."

# 정적 에셋 캐싱 (선택)
[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=3600"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=3600"
